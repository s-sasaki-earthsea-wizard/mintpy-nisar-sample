# Downloaded sample products (large, machine-local)
data/
*.h5
*.h5.part

# Python
__pycache__/
*.pyc

# reports/ is tracked -- it holds the committed validation evidence.
# The sample product is deterministic, so the report is machine-independent.
